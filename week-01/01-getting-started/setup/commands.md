# Setup Commands

```bash
npm install -g @offckb/cli
offckb --version
offckb node
```

To stop the node:

```text
CTRL+C
```

To clean local chain data after stopping the node:

```bash
offckb clean
```
